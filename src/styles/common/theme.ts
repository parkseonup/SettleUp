import { colors } from '../variables/colors';

export const theme = {
  default: {
    color: colors.BLACK,
    backgroundColor: colors.WHITE,
  },
  point: {
    color: colors.WHITE,
    backgroundColor: colors.POINT,
  },
};
